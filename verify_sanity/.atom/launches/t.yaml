# Cli launch configuration for bin/mutation_runner.dart.
type: cli
path: ~/GitRepos/flit/mutation_runner/lib/mutation_runner.dart

cli:
  # Additional args for the application.
  args:
  # The working directory to use for the launch.
  cwd:
  # Enable or disable checked mode.
  checked: true
  # Enable or disable debugging.
  debug: true
